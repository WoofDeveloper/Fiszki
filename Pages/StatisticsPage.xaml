<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:Fiszki.PageModels"
             x:Class="Fiszki.Pages.StatisticsPage"
             x:DataType="vm:StatisticsPageModel"
             Title="Statystyki"
             BackgroundColor="#F5F5F5">

    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="20">

            <ActivityIndicator IsRunning="{Binding IsLoading}" 
                               IsVisible="{Binding IsLoading}"
                               Color="#2196F3"
                               HeightRequest="50"/>

            <VerticalStackLayout IsVisible="{Binding IsLoading, Converter={StaticResource InvertedBoolConverter}}" 
                                 Spacing="15">

                <Border StrokeShape="RoundRectangle 12" 
                        BackgroundColor="White" 
                        Stroke="#E0E0E0" 
                        StrokeThickness="1"
                        Padding="16">
                    <Grid RowDefinitions="Auto,Auto" ColumnDefinitions="*,Auto">
                        <Label Grid.Row="0" Grid.Column="0"
                               Text="Wszystkich fiszek" 
                               FontSize="14"
                               TextColor="#757575"/>
                        <Label Grid.Row="1" Grid.Column="0"
                               Text="{Binding Statistics.TotalFlashcards}" 
                               FontSize="32"
                               FontAttributes="Bold"
                               TextColor="#2196F3"/>
                        <Image Grid.Row="0" Grid.RowSpan="2" Grid.Column="1"
                               Source="📚"
                               WidthRequest="40"
                               HeightRequest="40"
                               VerticalOptions="Center"/>
                    </Grid>
                </Border>

                <Border StrokeShape="RoundRectangle 12" 
                        BackgroundColor="White" 
                        Stroke="#E0E0E0" 
                        StrokeThickness="1"
                        Padding="16">
                    <Grid RowDefinitions="Auto,Auto" ColumnDefinitions="*,Auto">
                        <Label Grid.Row="0" Grid.Column="0"
                               Text="Opanowanych fiszek (≥80%)" 
                               FontSize="14"
                               TextColor="#757575"/>
                        <Label Grid.Row="1" Grid.Column="0"
                               Text="{Binding Statistics.MasteredFlashcards}" 
                               FontSize="32"
                               FontAttributes="Bold"
                               TextColor="#4CAF50"/>
                        <Label Grid.Row="0" Grid.RowSpan="2" Grid.Column="1"
                               Text="✅"
                               FontSize="40"
                               VerticalOptions="Center"/>
                    </Grid>
                </Border>

                <Border StrokeShape="RoundRectangle 12" 
                        BackgroundColor="White" 
                        Stroke="#E0E0E0" 
                        StrokeThickness="1"
                        Padding="16">
                    <Grid RowDefinitions="Auto,Auto" ColumnDefinitions="*,Auto">
                        <Label Grid.Row="0" Grid.Column="0"
                               Text="Do powtorki" 
                               FontSize="14"
                               TextColor="#757575"/>
                        <Label Grid.Row="1" Grid.Column="0"
                               Text="{Binding Statistics.DueForReview}" 
                               FontSize="32"
                               FontAttributes="Bold"
                               TextColor="#FF5722"/>
                        <Label Grid.Row="0" Grid.RowSpan="2" Grid.Column="1"
                               Text="🔄"
                               FontSize="40"
                               VerticalOptions="Center"/>
                    </Grid>
                </Border>

                <Border StrokeShape="RoundRectangle 12" 
                        BackgroundColor="White" 
                        Stroke="#E0E0E0" 
                        StrokeThickness="1"
                        Padding="16">
                    <Grid RowDefinitions="Auto,Auto" ColumnDefinitions="*,Auto">
                        <Label Grid.Row="0" Grid.Column="0"
                               Text="Studiowano dzisiaj" 
                               FontSize="14"
                               TextColor="#757575"/>
                        <Label Grid.Row="1" Grid.Column="0"
                               Text="{Binding Statistics.StudiedToday}" 
                               FontSize="32"
                               FontAttributes="Bold"
                               TextColor="#9C27B0"/>
                        <Label Grid.Row="0" Grid.RowSpan="2" Grid.Column="1"
                               Text="📅"
                               FontSize="40"
                               VerticalOptions="Center"/>
                    </Grid>
                </Border>

                <Border StrokeShape="RoundRectangle 12" 
                        BackgroundColor="White" 
                        Stroke="#E0E0E0" 
                        StrokeThickness="1"
                        Padding="16">
                    <Grid RowDefinitions="Auto,Auto" ColumnDefinitions="*,Auto">
                        <Label Grid.Row="0" Grid.Column="0"
                               Text="Łączna liczba powtórek" 
                               FontSize="14"
                               TextColor="#757575"/>
                        <Label Grid.Row="1" Grid.Column="0"
                               Text="{Binding Statistics.TotalReviews}" 
                               FontSize="32"
                               FontAttributes="Bold"
                               TextColor="#FF9800"/>
                        <Label Grid.Row="0" Grid.RowSpan="2" Grid.Column="1"
                               Text="🔢"
                               FontSize="40"
                               VerticalOptions="Center"/>
                    </Grid>
                </Border>

                <Border StrokeShape="RoundRectangle 12" 
                        BackgroundColor="White" 
                        Stroke="#E0E0E0" 
                        StrokeThickness="1"
                        Padding="16">
                    <Grid RowDefinitions="Auto,Auto" ColumnDefinitions="*,Auto">
                        <Label Grid.Row="0" Grid.Column="0"
                               Text="Średni wskaźnik sukcesu" 
                               FontSize="14"
                               TextColor="#757575"/>
                        <Label Grid.Row="1" Grid.Column="0"
                               FontSize="32"
                               FontAttributes="Bold"
                               TextColor="#00BCD4">
                            <Label.Text>
                                <MultiBinding StringFormat="{}{0:F1}%">
                                    <Binding Path="Statistics.AverageSuccessRate"/>
                                </MultiBinding>
                            </Label.Text>
                        </Label>
                        <Label Grid.Row="0" Grid.RowSpan="2" Grid.Column="1"
                               Text="📊"
                               FontSize="40"
                               VerticalOptions="Center"/>
                    </Grid>
                </Border>

                <Grid ColumnDefinitions="*,*" ColumnSpacing="10" Margin="0,10,0,0">
                    <Button Grid.Column="0"
                            Text="Odśwież"
                            BackgroundColor="#757575"
                            TextColor="White"
                            CornerRadius="8"
                            HeightRequest="50"
                            Command="{Binding RefreshCommand}"/>
                    
                    <Button Grid.Column="1"
                            Text="Zamknij"
                            BackgroundColor="#2196F3"
                            TextColor="White"
                            CornerRadius="8"
                            HeightRequest="50"
                            Command="{Binding CloseCommand}"/>
                </Grid>

            </VerticalStackLayout>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
